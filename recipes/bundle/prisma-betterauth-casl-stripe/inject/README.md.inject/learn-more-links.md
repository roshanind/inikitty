- [`ARCHITECTURE.md`](./ARCHITECTURE.md) — the tenancy → auth → RBAC → data access flow
- [`docs/adding-a-resource.md`](./docs/adding-a-resource.md) — the pattern for adding your first
  real resource, using `Projects` as the worked example
- API reference: `/api/docs` (Swagger/OpenAPI, once `api/` is running)
