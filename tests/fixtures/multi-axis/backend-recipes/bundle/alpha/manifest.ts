import type { RecipeManifest } from '../../../../../../src/engine/types.js';

export const manifest: RecipeManifest = {
  id: 'alpha',
  category: 'bundle',
  description: 'Fixture backend bundle used by generateMultiAxis tests.',
};
