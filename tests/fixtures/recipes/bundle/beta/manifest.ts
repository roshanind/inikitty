import type { RecipeManifest } from '../../../../../src/engine/types.js';

export const manifest: RecipeManifest = {
  id: 'beta',
  category: 'bundle',
  description: 'Second fixture bundle recipe, used to test requiresAnyOf across bundles.',
};
