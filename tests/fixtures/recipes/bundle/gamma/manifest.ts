import type { RecipeManifest } from '../../../../../src/engine/types.js';

export const manifest: RecipeManifest = {
  id: 'gamma',
  category: 'bundle',
  description: 'Third fixture bundle recipe — deliberately excluded from needs-alpha-or-beta\'s requiresAnyOf.',
};
