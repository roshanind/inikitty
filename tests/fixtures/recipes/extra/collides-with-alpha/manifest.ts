import type { RecipeManifest } from '../../../../../src/engine/types.js';

export const manifest: RecipeManifest = {
  id: 'collides-with-alpha',
  category: 'extra',
  description: 'Fixture recipe that deliberately writes the same file as the alpha bundle.',
};
